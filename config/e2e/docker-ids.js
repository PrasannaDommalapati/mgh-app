const prefix = 'hotel-app'

module.exports = {
    appId: `${prefix}.app`,
    apiId: `${prefix}.api`,
    seleniumId: `${prefix}.selenium`,
    protractorId: `${prefix}.protractor`,
    nodeChromeId: `${prefix}.node-chrome`,
};
