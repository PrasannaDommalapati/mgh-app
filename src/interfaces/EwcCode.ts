export interface EwcCode {
    code: string,
    unIdentificationNumber?: number,
    shippingName?: string,
    unClass?: string,
    packagingGroup?: string,
    handlingRequirements?: string,
    description: string,
}